# 日報 生成ルール（正本 / メモリ）

このファイルは「日報（#ai-日報 の夜レポート・PC日報）を作るときに**毎回必ず守る確定ルール**」の正本です。
要望メモ（index.html の業務要望メモ表）は“受付ログ”であり、ここに書かれたものが**実際のルール**です。
日報を生成するプログラム（統合夜レポート / PC日報生成 / 週次レポート）は、生成前に必ずこのファイルを参照すること。

最終更新: 2026-07-09

---

## ルール1【必須】日報に「お客様の声」を必ず入れる
- 出典データ: `knockout-weekly-sales-report/cs_summary.json`（cs_weekly が日曜20:00 に更新・push）。
- 日報本文に、以下を**必ず**載せる（分析だけで終わらせない）:
  1. `highlights`（総括コメント）— これまで本文に出力されておらず、抜けていた。**必ず出す**。
  2. 主要テーマ Top3 と件数（`top_themes`）、チャネル内訳（`by_channel`）、総件数／要対応件数。
  3. お客様が実際に困っている点（`problems`）と、そこから出た改善アクション（`improvements`）。
  4. 個別Q&Aの**原文全件**への導線（`sheet_url` = 顧客対応台帳リンク）。
- 週次だけでなく、**日次の日報にも**お客様の声セクションを差し込むこと（最新の cs_summary.json を要約表示）。
- 該当コード: `weekly_report.py` の「9. 顧客対応サマリ」(約 L359-378)。`highlights` を読み出して出力する処理が無いため追加が必要。

## ルール2【必須】やった内容は「PC作業」だけでなく「Claude（外出中）作業」も記録する
- 現状の日報は PC(Mac) の活動ログ（launchd の log-activity, `~/.local/logs/activity/`）だけを元データにしているため、
  **外出中に Claude 経由で行った作業（モバイル/Web の Claude・Claude Code セッション等）が丸ごと抜ける**。
- 日報の「やった内容」には、次の**両方**を必ず統合すること:
  1. PC活動ログ（従来どおり）。
  2. **Claude で行った作業ログ**（外出中含む）。当日の Claude セッションで実施・変更・push した内容を収集して記録する。
- どちらか一方しか取れない日でも、取れた方は必ず載せ、取れなかった方は「未取得」と明記する（黙って省かない）。

## ルール3【必須】推測で埋めない（事実のみ）
- 実データを確認できていない項目（各本部のKPI・売上・稼働実績など）は、**推測で埋めず「未確認」と明記**する。
- 「やってないのにやった風」を禁止。確認できた事実だけを、出典とともに書く。

---

## 反映が必要な場所（このルールを“実際の日報”に効かせるための作業）
- [x] `weekly_report.py`「9. 顧客対応サマリ」を「お客様の声・先週」に改称し、`highlights`（お客様の声・総括）を必ず出力するよう実装。
      → `knockout-weekly-sales-report` main に反映済み（2026-07-09, commit 922bb43）。次回の自動実行から有効。
- [ ] 日次の日報生成（統合夜レポート / PC日報生成 = Mac launchd・クラウドcron 側、GitHub外）に、
      ルール1（お客様の声）とルール2（Claude作業ログの統合）を実装。**このリポジトリからは触れないため、生成本体側での適用が別途必要。**
      当該コードを本セッションに追加できれば同様に実装する。

> 注意: この正本を更新しても、生成本体（上記チェックリスト）に実装しなければ“実際の日報”は変わらない。
> 「メモに記録した＝完了」ではない。生成本体への実装まで完了して初めてルールが効く。
